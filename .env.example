# AgentRouter 账号列表。使用 checkin-agentrouter add <name> 自动添加。
AGENTROUTER_ACCOUNTS=[]

# 本机 HTTP 或 mixed 代理端口。代理程序需要保持运行。
CHECKIN_PROXY_URL=http://127.0.0.1:7890
# 保持为一行 JSON，并使用双引号。
PROVIDERS={"agentrouter":{"domain":"https://agentrouter.org","use_proxy":true}}

# 可选：账号并发和 GitHub OAuth 并发
# CHECKIN_CONCURRENCY=3
# CHECKIN_OAUTH_CONCURRENCY=2

# 可选：配置后发送签到结果到飞书
# FEISHU_WEBHOOK=https://open.feishu.cn/open-apis/bot/v2/hook/replace-with-your-token

# 可选：配置后发送签到结果到 Telegram（需要同时填写机器人和会话 ID）
# 通知走下面的 CHECKIN_PROXY_URL，api.telegram.org 无法直连
# TELEGRAM_BOT_TOKEN=123456:replace-with-your-bot-token
# TELEGRAM_CHAT_ID=replace-with-your-chat-id

# 可选：每次运行都发送通知。默认 false，只在失败、首次运行或余额变化时通知
# ALWAYS_NOTIFY=false

# 可选：浏览器设置
# CHECKIN_HEADLESS=true
# CHECKIN_HUMANIZE=true
# CHECKIN_HUMANIZE_AGENTROUTER=true
# CHECKIN_WAIT_TIMEOUT_MS=60000
# CLOAKBROWSER_BINARY_PATH=/path/to/browser

# 可选：调试截图位置
# CHECKIN_SCREENSHOT_DIR=checkin_screenshots

# 可选：调试模式，截图只保存在本地
# DEBUG_MODE=false
